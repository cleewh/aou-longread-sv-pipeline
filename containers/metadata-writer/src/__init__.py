# metadata-writer package marker. Sources live at
# containers/metadata-writer/src/ in the repo; inside the image they are
# copied to /opt/metadata-writer/metadata_writer/ so that the entry
# point `python -m metadata_writer` resolves cleanly.
